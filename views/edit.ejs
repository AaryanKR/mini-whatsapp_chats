<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <h2>edit your chat : <%= chat._id %></h2>
    <p>this chat was sent from <b><i><%= chat.from %></i></b> to <i><b><%= chat.to %></i></b></p>
    <form method="post" action="/chats/<%= chat._id %>?_method=put">
        <textarea name="msg" rows="5" cols="25"><%= chat.msg %></textarea>
        <br />
        <button>edit</button>
    </form>
</body>
</html>